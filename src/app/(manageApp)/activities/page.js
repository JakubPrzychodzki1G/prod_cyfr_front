import Activities from "./components/activities"

export const metadata = {
  title: "Wydarzenia",
  description: "Strona z wydarzeniami w klubie",
  // other metadata
}

const System = () => {
    return (
        <Activities />
    )
}

export default System
