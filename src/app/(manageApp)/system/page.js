import Settings from "./components/settings"

export const metadata = {
  title: "Settings Page",
  description: "Strona ustawień systemowych",
  // other metadata
}

const System = () => {
    return (
        <Settings />
    )
}

export default System
